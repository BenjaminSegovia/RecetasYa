server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5433/auth_db
    username: recetaya
    password: recetaya123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Clave usada para FIRMAR los JWT. Debe ser EXACTAMENTE la misma
# en receta-service, inventory-service y dispensing-service (ahí se usa
# para VALIDAR el token), o la verificación va a fallar.
jwt:
  secret: recetasyaduocclantoniovaras12345678
  expiration-ms: 3600000